// import f from 'odata-filter-builder'

// import { GET, POST, RockApiError } from '../../client'

// let GroupRoleId: number

export async function getFamilyRole(): Promise<void> {
  //   householdRole: string,
  //   personId: number,
  //   fluroId: string
  // transform housrhold Role into rock role, write transformer function
  // if null default to 'none'
  // get group role ID by filtering groups by household Role
  // if no household role found but householdROle value is not null, create a new value and return the groupRoleID
}
